const int v2 = 0; int v1 = v2;                   顶层const
int *p1 = &v1, &r1 = v1;                         不是const
const int *p2 = &v2, *const p3 = &i, &r2 = v2;   p2是底层const；p3最左是底层，p3前面是顶层const；r2是底层const